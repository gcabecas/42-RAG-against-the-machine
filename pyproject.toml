[project]
name = "rag"
version = "1.0.0"
description = "rag"
requires-python = ">=3.10"
dependencies = [
    "fire",
    "flake8",
    "mypy",
    "pydantic"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/student"]
